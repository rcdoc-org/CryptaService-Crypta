version: "3.8"
services:
  #  nginx:
  # image: nginx:stable
  # volumes:
  #   - ./deploy/nginx.conf:/etc/nginx/nginx.conf:ro
  #   - ./frontend/build:/usr/share/nginx/html:ro
  # ports:
  #   - "443:443"
  # depends_on:
  #   - frontend
  #   - auth-api
  #   - gateway

  frontend:
    build: ./frontend
    command: yarn dev         # or `npm start` for hot‐reload
    ports:
      - "80:80"
    environment:
      - CHOKIDAR_USEPOLLING=true  # on Linux for webpack

  auth-api:
    build: ./services/auth
    command: uvicorn server:app --host 0.0.0.0 --port 8002
    ports:
      - "8002:8002"
    env_file:
      - ./services/auth/.env

  gateway:
    build: ./services/gateway
    command: uvicorn server:server --host 0.0.0.0 --port 3000
    ports:
      - "3000:3000"
    env_file:
      - ./services/gateway/.env
